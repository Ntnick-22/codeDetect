# SNS Architecture & Flow Diagram

## The Complete Picture

### Current State (BEFORE FIX)

```
┌─────────────────────────────────────────────────────────────────┐
│                        USER EXPERIENCE                           │
└─────────────────────────────────────────────────────────────────┘
    │
    │ 1. User clicks "Report Issue"
    │ 2. Fills form (email, type, message)
    │ 3. Clicks Submit
    ↓
┌─────────────────────────────────────────────────────────────────┐
│  FRONTEND (frontend/report-system.js:46-58)                     │
│                                                                  │
│  fetch('/api/report', {                                         │
│    method: 'POST',                                              │
│    body: JSON.stringify({                                       │
│      email: email,                                              │
│      type: type,                                                │
│      message: message                                           │
│    })                                                           │
│  })                                                             │
└─────────────────────────────────────────────────────────────────┘
    │
    │ HTTP POST to /api/report
    ↓
┌─────────────────────────────────────────────────────────────────┐
│  BACKEND (backend/app.py:394-454)                               │
│                                                                  │
│  @app.route('/api/report', methods=['POST'])                    │
│  def submit_report():                                           │
│      data = request.get_json()                                  │
│      email = data.get('email')                                  │
│      message = data.get('message')                              │
│                                                                  │
│      # THIS IS WHERE IT FAILS! ❌                               │
│      topic_arn = os.environ.get('SNS_TOPIC_ARN')  ← LINE 429   │
│                                                                  │
│      if not topic_arn:                                          │
│          return jsonify({'error': 'SNS not configured'}), 500   │
│          ↑                                                       │
│          THIS ERROR IS RETURNED                                 │
└─────────────────────────────────────────────────────────────────┘
    │
    │ os.environ.get('SNS_TOPIC_ARN') returns None
    │ Why? Environment variable not set!
    ↓
┌─────────────────────────────────────────────────────────────────┐
│  ENVIRONMENT VARIABLES (os.environ dict)                        │
│                                                                  │
│  ✅ FLASK_ENV = 'production'                                    │
│  ✅ S3_BUCKET_NAME = 'codedetect-nick-uploads-12345'            │
│  ✅ SECRET_KEY = 'xxx...'                                       │
│  ❌ SNS_TOPIC_ARN = None  ← MISSING!                            │
└─────────────────────────────────────────────────────────────────┘
    │
    │ Where should this come from?
    ↓
┌─────────────────────────────────────────────────────────────────┐
│  ON AWS EC2: entrypoint.sh (backend/entrypoint.sh:103-107)     │
│                                                                  │
│  # Fetch SNS Topic ARN for user feedback                        │
│  SNS_ARN=$(get_parameter "codedetect-prod-sns-feedback-topic-arn") │
│  if [ -n "$SNS_ARN" ]; then                                     │
│      export SNS_TOPIC_ARN="$SNS_ARN"                            │
│  fi                                                             │
└─────────────────────────────────────────────────────────────────┘
    │
    │ Tries to fetch from AWS Parameter Store
    ↓
┌─────────────────────────────────────────────────────────────────┐
│  AWS PARAMETER STORE                                            │
│  (AWS Systems Manager)                                          │
│                                                                  │
│  Parameters:                                                    │
│  ✅ /codedetect/prod/flask/secret_key                           │
│  ✅ /codedetect/prod/s3/bucket_name                             │
│  ✅ /codedetect/prod/database/url                               │
│  ❌ /codedetect/prod/sns/feedback-topic-arn  ← DOESN'T EXIST!  │
└─────────────────────────────────────────────────────────────────┘
    │
    │ Parameter doesn't exist!
    │ Why? Never created in Terraform
    ↓
    RESULT: Application can't send reports ❌
```

---

## New State (AFTER FIX)

```
┌─────────────────────────────────────────────────────────────────┐
│                    INFRASTRUCTURE SETUP                          │
│                     (terraform apply)                            │
└─────────────────────────────────────────────────────────────────┘
    │
    │ terraform apply
    ↓
┌─────────────────────────────────────────────────────────────────┐
│  TERRAFORM (terraform/sns.tf)                                   │
│                                                                  │
│  resource "aws_sns_topic" "user_feedback" {                     │
│    name = "codedetect-prod-user-feedback"                       │
│  }                                                              │
│                                                                  │
│  resource "aws_sns_topic_subscription" "user_feedback_email" {  │
│    topic_arn = aws_sns_topic.user_feedback.arn                  │
│    protocol  = "email"                                          │
│    endpoint  = "nyeinthunaing322@gmail.com"                     │
│  }                                                              │
└─────────────────────────────────────────────────────────────────┘
    │
    │ Creates in AWS
    ↓
┌─────────────────────────────────────────────────────────────────┐
│  AWS SNS TOPIC                                                  │
│  ARN: arn:aws:sns:eu-west-1:772297676546:codedetect-prod-user-feedback │
│                                                                  │
│  Subscriptions:                                                 │
│  📧 nyeinthunaing322@gmail.com (email)                          │
│     Status: ⏳ PendingConfirmation → ✅ Confirmed               │
└─────────────────────────────────────────────────────────────────┘
    │
    │ terraform/secrets.tf creates Parameter Store entry
    ↓
┌─────────────────────────────────────────────────────────────────┐
│  TERRAFORM (terraform/secrets.tf:236-250)                       │
│                                                                  │
│  resource "aws_ssm_parameter" "sns_feedback_topic_arn" {        │
│    name  = "/codedetect/prod/sns/feedback-topic-arn"            │
│    value = aws_sns_topic.user_feedback.arn                      │
│  }                                                              │
└─────────────────────────────────────────────────────────────────┘
    │
    │ Stores in AWS Parameter Store
    ↓
┌─────────────────────────────────────────────────────────────────┐
│  AWS PARAMETER STORE                                            │
│                                                                  │
│  Parameters:                                                    │
│  ✅ /codedetect/prod/flask/secret_key = "xxx..."                │
│  ✅ /codedetect/prod/s3/bucket_name = "codedetect-nick-..."     │
│  ✅ /codedetect/prod/database/url = "sqlite:///..."             │
│  ✅ /codedetect/prod/sns/feedback-topic-arn = "arn:aws:sns:..." │
│      ↑                                                           │
│      NOW IT EXISTS! ✅                                          │
└─────────────────────────────────────────────────────────────────┘

═══════════════════════════════════════════════════════════════════
        INFRASTRUCTURE READY - NOW LET'S USE IT
═══════════════════════════════════════════════════════════════════

┌─────────────────────────────────────────────────────────────────┐
│  EC2 INSTANCE STARTUP                                           │
└─────────────────────────────────────────────────────────────────┘
    │
    │ Docker container starts
    ↓
┌─────────────────────────────────────────────────────────────────┐
│  ENTRYPOINT SCRIPT (backend/entrypoint.sh)                      │
│                                                                  │
│  # Load secrets from Parameter Store                            │
│  SECRET_KEY=$(get_parameter "codedetect-prod-flask-secret_key") │
│  S3_BUCKET=$(get_parameter "codedetect-prod-s3-bucket_name")    │
│  SNS_ARN=$(get_parameter "codedetect-prod-sns-feedback-topic-arn") │
│      ↑                                                           │
│      Fetches from Parameter Store                               │
│                                                                  │
│  # Export as environment variables                              │
│  export SECRET_KEY="$SECRET_KEY"                                │
│  export S3_BUCKET_NAME="$S3_BUCKET"                             │
│  export SNS_TOPIC_ARN="$SNS_ARN"  ← NOW SET! ✅                 │
└─────────────────────────────────────────────────────────────────┘
    │
    │ Variables loaded into environment
    ↓
┌─────────────────────────────────────────────────────────────────┐
│  ENVIRONMENT VARIABLES (os.environ)                             │
│                                                                  │
│  ✅ FLASK_ENV = 'production'                                    │
│  ✅ S3_BUCKET_NAME = 'codedetect-nick-uploads-12345'            │
│  ✅ SECRET_KEY = 'xxx...'                                       │
│  ✅ SNS_TOPIC_ARN = 'arn:aws:sns:eu-west-1:...'  ← PRESENT! ✅  │
└─────────────────────────────────────────────────────────────────┘
    │
    │ Flask app starts
    ↓
┌─────────────────────────────────────────────────────────────────┐
│  FLASK APPLICATION RUNNING                                      │
│  Ready to handle requests                                       │
└─────────────────────────────────────────────────────────────────┘

═══════════════════════════════════════════════════════════════════
        USER SUBMITS REPORT - THE HAPPY PATH ✅
═══════════════════════════════════════════════════════────════════

┌─────────────────────────────────────────────────────────────────┐
│  USER                                                           │
│  1. Clicks "Report Issue"                                       │
│  2. Fills form                                                  │
│  3. Submits                                                     │
└─────────────────────────────────────────────────────────────────┘
    │
    │ POST /api/report
    ↓
┌─────────────────────────────────────────────────────────────────┐
│  BACKEND (backend/app.py:394-454)                               │
│                                                                  │
│  @app.route('/api/report', methods=['POST'])                    │
│  def submit_report():                                           │
│      email = data.get('email')                                  │
│      message = data.get('message')                              │
│                                                                  │
│      # Get SNS topic ARN from environment                       │
│      topic_arn = os.environ.get('SNS_TOPIC_ARN')  ← LINE 429   │
│      # Returns: arn:aws:sns:eu-west-1:...  ✅                   │
│                                                                  │
│      if not topic_arn:  ← FALSE, we have it!                    │
│          # This won't execute anymore                           │
│                                                                  │
│      # Publish to SNS                                           │
│      sns = boto3.client('sns', region_name='eu-west-1')         │
│      response = sns.publish(                                    │
│          TopicArn=topic_arn,                                    │
│          Subject='CodeDetect: BUG REPORT',                      │
│          Message=sns_message                                    │
│      )                                                          │
│      return jsonify({'success': True}), 200  ✅                 │
└─────────────────────────────────────────────────────────────────┘
    │
    │ Publishes message to SNS
    ↓
┌─────────────────────────────────────────────────────────────────┐
│  AWS SNS                                                        │
│  Topic: codedetect-prod-user-feedback                           │
│                                                                  │
│  Received message:                                              │
│  Subject: CodeDetect: BUG REPORT                                │
│  Message: User reported issue...                                │
└─────────────────────────────────────────────────────────────────┘
    │
    │ Looks up subscriptions
    ↓
┌─────────────────────────────────────────────────────────────────┐
│  SNS SUBSCRIPTION                                               │
│  Protocol: email                                                │
│  Endpoint: nyeinthunaing322@gmail.com                           │
│  Status: ✅ Confirmed                                           │
└─────────────────────────────────────────────────────────────────┘
    │
    │ Sends email
    ↓
┌─────────────────────────────────────────────────────────────────┐
│  GMAIL INBOX                                                    │
│                                                                  │
│  From: AWS SNS <no-reply@sns.amazonaws.com>                     │
│  Subject: CodeDetect: BUG REPORT                                │
│  Body: User reported issue...                                   │
│                                                                  │
│  [Because you whitelisted @sns.amazonaws.com, it goes to inbox] │
└─────────────────────────────────────────────────────────────────┘
    │
    │ You read email and respond to user
    ↓
    SUCCESS! ✅
```

---

## Why Auto-Unsubscribe Happens

```
┌─────────────────────────────────────────────────────────────────┐
│  AWS SNS sends email                                            │
│  To: nyeinthunaing322@gmail.com                                 │
└─────────────────────────────────────────────────────────────────┘
    │
    ↓
┌─────────────────────────────────────────────────────────────────┐
│  GMAIL SERVER                                                   │
│                                                                  │
│  Option 1: Accepts email → Inbox ✅                             │
│  Option 2: Spam filter triggers → Spam folder ⚠️                │
│  Option 3: Server rejects (bounce) ❌                           │
└─────────────────────────────────────────────────────────────────┘
    │
    │ If Option 2 or 3 happens...
    ↓
┌─────────────────────────────────────────────────────────────────┐
│  GMAIL SENDS FEEDBACK TO AWS                                    │
│                                                                  │
│  Feedback Loop:                                                 │
│  - "This email was marked as spam"                              │
│  - "This email bounced"                                         │
│  - "User complained about this sender"                          │
└─────────────────────────────────────────────────────────────────┘
    │
    │ AWS receives bounce/complaint
    ↓
┌─────────────────────────────────────────────────────────────────┐
│  AWS SNS DELIVERABILITY PROTECTION                              │
│                                                                  │
│  if (email bounced || spam complaint):                          │
│      automatically_unsubscribe(email)                           │
│      # Protects AWS sender reputation                           │
└─────────────────────────────────────────────────────────────────┘
    │
    ↓
    Your subscription is removed ❌

    SOLUTION: Whitelist @sns.amazonaws.com in Gmail ✅
```

---

## Local Development Flow

```
┌─────────────────────────────────────────────────────────────────┐
│  YOUR COMPUTER (Local Development)                              │
└─────────────────────────────────────────────────────────────────┘
    │
    │ You create .env file
    ↓
┌─────────────────────────────────────────────────────────────────┐
│  .env FILE                                                      │
│                                                                  │
│  FLASK_ENV=development                                          │
│  S3_BUCKET_NAME=codedetect-nick-uploads-12345                   │
│  SNS_TOPIC_ARN=arn:aws:sns:eu-west-1:...                        │
└─────────────────────────────────────────────────────────────────┘
    │
    │ Load with: source .env
    │ OR docker-compose automatically loads it
    ↓
┌─────────────────────────────────────────────────────────────────┐
│  ENVIRONMENT VARIABLES (on your computer)                       │
│  ✅ All values from .env file                                   │
└─────────────────────────────────────────────────────────────────┘
    │
    ↓
┌─────────────────────────────────────────────────────────────────┐
│  FLASK APP RUNNING LOCALLY                                      │
│  Can now send SNS messages                                      │
└─────────────────────────────────────────────────────────────────┘
```

---

## Summary - The Chain of Dependencies

```
TERRAFORM
   ↓ creates
SNS TOPIC (in AWS)
   ↓ ARN stored in
PARAMETER STORE (in AWS)
   ↓ loaded by
ENTRYPOINT.SH (on EC2 startup)
   ↓ exports as
ENVIRONMENT VARIABLE (SNS_TOPIC_ARN)
   ↓ read by
FLASK APP (backend/app.py:429)
   ↓ publishes to
SNS TOPIC
   ↓ emails
YOUR INBOX
   ↓ (if whitelisted)
INBOX (not spam) ✅
```

Every link in this chain must work for the system to function!

**What was broken before**: Parameter Store entry missing → Chain breaks at step 3

**What I fixed**: Added Terraform to create the Parameter Store entry → Chain complete ✅

---

## Files Map

```
codeDetect/
│
├── backend/
│   ├── app.py                    ← LINE 429: reads SNS_TOPIC_ARN
│   └── entrypoint.sh              ← LINE 103-107: loads from Parameter Store
│
├── terraform/
│   ├── sns.tf                     ← NEW: Creates SNS topic
│   ├── secrets.tf                 ← MODIFIED: Adds SNS ARN to Parameter Store
│   └── outputs.tf                 ← MODIFIED: Shows SNS ARN after apply
│
├── .env                           ← NEW: For local development
├── .env.example                   ← MODIFIED: Template with SNS config
│
├── QUICK_FIX_SUMMARY.md          ← Quick reference
├── SNS_FIX_GUIDE.md              ← Detailed documentation
└── SNS_ARCHITECTURE_FLOW.md      ← This file (visual diagrams)
```

---

## Next: Apply the Fix

```bash
# 1. Apply Terraform
cd terraform
terraform apply

# 2. Confirm email subscription

# 3. Whitelist in Gmail

# 4. Test!
```
